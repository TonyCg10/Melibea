[package]
name = "melibea"
version = "0.1.0"
edition = "2024"
description = "Deterministic attention and surface-state controller for niri"
license = "MIT"
publish = false

[dependencies]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"

